Django==5.0.6
# psycopg2-binary requiere postgresql-devel instalado en el sistema.
# Si falla la instalación (Python 3.14+), instala manualmente:
#   pip install "psycopg[binary]"
psycopg2-binary>=2.9.9
Pillow==10.3.0
